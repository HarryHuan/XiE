# XiEngine 羲引擎 统一编码&分层规范
> 本规则全局永久生效，AI生成代码、项目迭代、模块编写、底层渲染迁移必须严格遵守

## 一、层级前缀映射对照表
| 前缀  | 定名  | 对标UE标识 |  代码层级  | 适用范围                                                                                              |
| :---: | :---: | :--------: | :--------: | ----------------------------------------------------------------------------------------------------- |
|  Yao  | 羲爻  |     F      | 底层内核层 | 纯C++无反射结构体/类、数学库、RHI、Vulkan/Metal跨平台封装、Shader、文件IO、网络、内存管理、底层工具库 |
|  Du   | 羲牍  |     U      | 资源管理层 | 带反射、GC、序列化、资产文件、网格/贴图/材质、资源管理器、配置数据                                    |
|  Lu   | 羲箓  |     A      | 场景运行层 | 场景Actor实体、角色、光源、碰撞体、关卡对象、蓝图逻辑载体                                             |
|  Yan  | 羲砚  |     S      |  编辑器层  | 编辑器窗口、原生UI控件、视口、编辑面板、编辑器交互组件                                                |

## 二、统一代码命名写法
1. 类/结构体：大驼峰 `YaoRHIBuffer` / `DuMaterial` / `LuActor` / `YanViewport`
2. 变量规范
   - 实例成员：`m_xxx`
   - 静态成员：`s_xxx`
   - 全局变量：`g_xxx`
3. 命名空间强制包裹
```cpp
namespace Xi::Yao
namespace Xi::Du
namespace Xi::Lu
namespace Xi::Yan
```

## 三、其他
- 每一步实现都要讲解清楚
- 每一个方法或者变量都要备注清晰
- 推送时，message格式如下：
以下中括号中的数字不停的累加
如果是新特性
```
feat[00001]: 此次总结
此次修改列表
```
如果是bug修复
```
fix[00001]: bug简介，不超过20个字
如何修复的bug
```